import React from 'react'

const AddLocationCurrentLocation = (props) => {
  return (
    <div>
        AddLocationCurrentLocation
        <br></br>
        <button onClick={() => props.toggleClose()}>Close</button>
    </div>
  )
}

export default AddLocationCurrentLocation